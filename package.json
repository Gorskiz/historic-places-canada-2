{
	"name": "historic-places-canada",
	"version": "1.0.0",
	"private": true,
	"description": "Full-stack Historic Places Canada app on Cloudflare Workers",
	"scripts": {
		"build": "node build.js",
		"deploy": "npm run build && wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.60.0"
	}
}